# V4.0.7 shader integrity

The complete tested GLES2 shader is packaged in the private recovery archive as:

`patches/fragment_region_v407.glsl`

SHA-256:

`00922001dcbdbfb8524ab08fb7378ed0c88408ce937ac8594f279bf9dc8ceaea`

It contains the marker `JingHua V4.0.7 recovery shader` and passed real EGL/GLES2 compile and link validation. The private recovery archive also contains the comparison evidence, regression scripts, APK signing tools, and signing keystore. Do not commit the signing keystore to a public repository.
