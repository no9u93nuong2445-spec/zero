name: Archived Package JingHua V4.0.4

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived on the V4.0.8 test branch; use the dedicated V4.0.8 workflow.'
